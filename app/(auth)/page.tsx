// components
import ChooseLogin from "@/components/loginPage/ChooseLogin";

const Login = () => {
  return (
    <main className="background flex items-center justify-center min-h-screen">
      <ChooseLogin />
    </main>
  );
};

export default Login;
